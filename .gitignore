node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite

# Audio library (~4.9 GB) — hosted on Cloudflare R2, never committed.
# tracks/songs.json and tracks/README.md ARE tracked (they're tiny and the app
# imports the manifest at build time).
tracks/Easy/
tracks/Medium/
tracks/Hard/

# Stray Vite config timestamp artifacts
vite.config.js.timestamp-*
